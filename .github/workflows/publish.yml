name: Release
on: 
#  push: 
#    tags: 
#      - 'v*.*.*'
  release:
    types:
      - published
jobs:
  build:
    runs-on: macos-latest
    steps:
    - name: Info
      run: |
        whoami
        echo "==========================\n"
        df
        echo "==========================\n"
        mount
        echo "==========================\n"
        pwd
        echo "==========================\n"
        sysctl machdep.cpu
        echo "==========================\n"
        ifconfig
